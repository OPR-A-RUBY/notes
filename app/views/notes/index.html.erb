<p id="notice"><%= notice %></p>

<div class="border border-info" >
  <nav class="navbar navbar-light" style="background-color: #e3f2fd;">
    <div class="text-left col-md-11 col-sm-10">
      <a class="navbar-brand" href="/notes">Notes</a>
      <%= link_to 'New Note', new_note_path, class: 'btn btn-outline-primary btn-sm' if user_signed_in? %>
    </div>         
    <div class="text-right col-md-1 col-sm-2"> 
      <%= link_to ' Home ',      root_path, class: 'btn btn-outline-info btn-sm' %>
    </div>
  </nav>
</div>

<% stars_display %>

                                                  <% 3.times do |i|          %>
<table class="table">
    <thead class="thead-light"> 
      <tr>
        <th>Id</th>
        <th>Title</th>
        <th>Description</th>
      </tr>
    </thead>
                                                  <%  if    i == 0          %>
                                                  <%    hh_name = :open     %>
                                                  <%  elsif i == 1          %>
                                                  <%    hh_name = :closed   %>
                                                  <%  elsif i == 2          %>
                                                  <%    hh_name = :public   %>
                                                  <%  end                   %>
    <tbody>
      <% @notes_3[hh_name].each do |note| %> <!--  -->
        <tr>
          <td><%= note.id %></td>
          <td><b><%= note.title %></b></td>
          <td>
            <%= note.description %><br />
              <div class='text-right'>
                <%= link_to 'Show', note, class: 'btn btn-outline-success btn-sm' %>
                <%= link_to 'Link to', note.url, class: 'btn btn-outline-primary btn-sm' %>
                <% if user_signed_in? %>             <!-- выводить DELETE & EDIT только если КЛИЕНТ, а дальше -->
                  <% if current_user.id == note.user_id %> <!-- выводить DELETE & EDIT если КЛИЕНТ есть АВТОР -->
                    <%= link_to 'Edit', edit_note_path(note), class: 'btn btn-outline-warning btn-sm'%>
                    <%= link_to 'Destroy', note, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-outline-danger btn-sm' %>
                  <% end %> <!-- /if current... -->
                <% end %> <!-- /if user... -->  
              </div>
            </tr>
          </td> 
        </tr>
      <% end %>
    </tbody>
  </table>
                                                  <% end                    %>

<% if user_signed_in? %> <!-- ЕСЛИ КЛИЕНТ: то выводим две следующие таблицы: -->
  
  <h4 class="text-left">It is My Private notes: </h4>

  <table class="table">
    <thead class="thead-light">             <!-- ПЕРВАЯ ТАБЛИЦА: - этого клиента (скрытые) -->
      <tr>
        <th>Id</th>
        <th>Title</th>
        <th>Description</th>
      </tr>
    </thead>

    <tbody>
      <% @notes_3[:closed].each do |note| %> <!-- СКРЫТЫЕ -->
        <tr>
          <td><%= note.id %></td>
          <td><b><%= note.title %></b></td>
          <td>
            <%= note.description %><br />
              <div class='text-right'>
                <%= link_to 'Show', note, class: 'btn btn-outline-success btn-sm' %>
                <%= link_to 'Link to', note.url, class: 'btn btn-outline-primary btn-sm' %>
                <% if user_signed_in? %>             <!-- выводить DELETE & EDIT только если КЛИЕНТ, а дальше -->
                  <% if current_user.id == note.user_id %> <!-- выводить DELETE & EDIT если КЛИЕНТ есть АВТОР -->
                    <%= link_to 'Edit', edit_note_path(note), class: 'btn btn-outline-warning btn-sm'%>
                    <%= link_to 'Destroy', note, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-outline-danger btn-sm' %>
                  <% end %> <!-- /if current... -->
                <% end %> <!-- /if user... -->  
              </div>
            </tr>
          </td> 
        </tr>
      <% end %>
    </tbody>
  </table>


  <h4 class="text-left">It is My Public notes: </h4>

  <table class="table">
    <thead class="thead-light">             <!-- ВТОРАЯ ТАБЛИЦА: - этого клиента (открытые) -->
      <tr>
        <th>Id</th>
        <th>Title</th>
        <th>Description</th>
      </tr>
    </thead>

    <tbody>
      <% @notes_3[:open].each do |note| %> <!-- ОТКРЫТЫЕ -->
        <tr>
          <td><%= note.id %></td>
          <td><b><%= note.title %></b></td>
          <td>
            <%= note.description %><br />
              <div class='text-right'>
                <%= link_to 'Show', note, class: 'btn btn-outline-success btn-sm' %>
                <%= link_to 'Link to', note.url, class: 'btn btn-outline-primary btn-sm' %>
                <% if user_signed_in? %>             <!-- выводить DELETE & EDIT только если КЛИЕНТ, а дальше -->
                  <% if current_user.id == note.user_id %> <!-- выводить DELETE & EDIT если КЛИЕНТ есть АВТОР -->
                    <%= link_to 'Edit', edit_note_path(note), class: 'btn btn-outline-warning btn-sm'%>
                    <%= link_to 'Destroy', note, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-outline-danger btn-sm' %>
                  <% end %> <!-- /if current... -->
                <% end %> <!-- /if user... -->  
              </div>
            </tr>
          </td> 
        </tr>
      <% end %>
    </tbody>
  </table> 
<% end %> <!-- /if user... -->

                            <!-- ДАЛЬШЕ всегда выводим таблицу и для КЛИЕНТА, и для ГОСТЯ --> 
<h4 class="text-left"> It is Public notes of all other users: </h4>

<table class="table">
  <thead class="thead-light">                 <!-- ТРЕТЬЯ ТАБЛИЦА: - чужие публичные записки -->
    <tr>                                      <!-- для ГОСТЯ все публичные записки есть "чужие" -->   
        <th>Id</th>
        <th>Title</th>
        <th>Description</th>
    </tr>
  </thead>

  <tbody>
    <% @notes_3[:public].each do |note| %>
      <tr>
          <td><%= note.id %></td>
          <td><b><%= note.title %></b></td>
          <td>
            <%= note.description %><br />
              <div class='text-right'>
                <%= link_to 'Show', note, class: 'btn btn-outline-success btn-sm' %>
                <%= link_to 'Link to', note.url, class: 'btn btn-outline-primary btn-sm' %>
                <% if user_signed_in? %>             <!-- выводить DELETE & EDIT только если КЛИЕНТ, а дальше -->
                  <% if current_user.id == note.user_id %> <!-- выводить DELETE & EDIT если КЛИЕНТ есть АВТОР -->
                    <%= link_to 'Edit', edit_note_path(note), class: 'btn btn-outline-warning btn-sm'%>
                    <%= link_to 'Destroy', note, method: :delete, data: { confirm: 'Are you sure?' }, class: 'btn btn-outline-danger btn-sm' %>
                  <% end %> <!-- /if current... -->
                <% end %> <!-- /if user... -->  
              </div>
            </tr>
          </td> 
      </tr>
    <% end %>
  </tbody>
</table>