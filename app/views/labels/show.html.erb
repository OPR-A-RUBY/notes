<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @label.name %>
</p>

<p>
  <strong>User:</strong>
  <%= @label.user_id %>
</p>

<p>
  <strong>Description:</strong>
  <%= @label.description %>
</p>

<%= link_to 'Back', labels_path, class: 'btn btn-info btn-sm', title: 'Show labels' %>
<%= link_to 'Edit', edit_label_path(@label), class: 'btn btn-outline-warning btn-sm'%>
